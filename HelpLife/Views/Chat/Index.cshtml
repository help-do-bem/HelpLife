@model IList<Chat>

 
    @foreach (var item in Model)
    {
        <div id="msg" class="@item.Tipo">
            <p id="username">@item.Nome</p>
            <p>@item.Texto</p>
            <p>@item.createdAt</p>
        </div>
    }
 